# 로드맵 — 목표 / 세부목표 / 실행과제

> 이 문서는 `구술_아카이브_서비스_기획_정리노트.md`(이하 "정리노트")와 `progress.md`에 흩어져 있던 미해결·미정 사항들을 근거로 만들었습니다. 정리노트 10번 "일정(로드맵/WBS) — 미착수"에 해당하는 문서입니다.
>
> **구조**: 목표 A는 "정리노트 자체를 명확한 정책으로 확정하는 것"이고, 목표 B는 "확정된 정책을 실제로 구현하는 순서"입니다. 실행과제가 코드를 만드는 일보다 **결정을 내리는 일**부터 시작하는 이유는, 지금 정리노트에 미결정 항목이 많아서 무엇을 만들지 자체가 아직 명확하지 않기 때문입니다 — 결정 없이 구현부터 시작하면 다시 갈아엎을 위험이 큽니다(연표 화면이 이미 두 번 갈아엎어진 전례가 있음, progress.md §5).
>
> 각 실행과제에는 **[결정 필요]** 또는 **[실행]** 태그를 붙였습니다. [결정 필요] 항목에는 제 판단으로 만든 **제안(초안)**을 달아뒀습니다 — 그대로 채택하거나, 수정하거나, 다음 대화에서 논의해서 확정하면 됩니다. 확정되면 정리노트의 해당 섹션을 갱신하는 것까지가 그 실행과제의 완료 조건입니다.

---

## 목표 트리 한눈에 보기

```
목표 A. 기획 정책 확정 (정리노트의 미해결·미정 사항 해소)
├─ A1. 자료 입력 정책 확정 (CSV 오류처리, 발화자 구조)
├─ A2. 스키마 미결정 사항 확정 (인물전거, 발화자 필드, 장소좌표, 공개범위)
├─ A3. IA 공백 확정 (검색창, 통합타임라인 개념, UI레퍼런스 적용처, 사이드패널 방향) — 로그인 범위는 확정됨
└─ A4. 외부데이터 이용정책 확정 (§3-6 기관별 표 신설, 재배포 범위, 문서 오류 정정)

목표 B. 정책을 반영한 실행 로드맵 (Phase 1~4)
├─ B1. Phase 1 — 스키마 확정 기반 Supabase 전환
├─ B2. Phase 2 — 자료 등록/관리 워크플로우
├─ B3. Phase 3 — 커버리지 확장 + 경험 개선
└─ B4. Phase 4 — 2단계(엔진 공유) 준비
```

---

## 목표 A. 기획 정책 확정

### A1. 자료 입력 정책 확정 (정리노트 §5-2-1)

**세부목표**: 자료 등록 화면을 만들기 전에, 지금 "질문만 던지고 답을 못 정한 채 넘어간" 두 가지를 정책으로 확정한다.

- [ ] **[결정 필요] CSV 업로드 오류 처리 방식** (정리노트 §5-2-1, §8-5)
  질문: 업로드한 CSV에 오류 행이 있으면 (a) 전체 업로드 차단 후 재업로드 vs (b) 오류 행만 제외하고 나머지 정상 등록 + 실패 목록 안내
  **제안**: (b). 자료가 계속 누적되는 운영 방식상 한 줄 오류로 전체가 막히면 반복 비용이 큼. "확인 안 된 건 확인 안 된 대로 노출"이라는 서비스 원칙(정리노트 §1)과도 결이 맞음.
  완료 조건: 정리노트 §5-2-1에서 이 항목 제거하고 §5-1에 정책 문장 추가

- [ ] **[결정 필요] 면담자-구술자 대화 반복 구조** (정리노트 §5-2-1, §8-6, §6-1-1)
  질문: 발췌 구간 하나에 질문-답변이 한 번만 있는지, 여러 번 오가는 대화 전체가 한 구간이 될 수 있는지
  **제안**: 여러 번 오가는 구조를 허용. `segments.segment_text` 단일 필드를 발화 단위 배열(`[{speaker_role: 면담자|구술자|지문, text}]`)로 구조화 — 화면의 색 구분(정리노트 §7-2)을 파싱이 아니라 데이터로 직접 뒷받침.
  완료 조건: 정리노트 §6-1-1의 관련 문단 삭제, §6-1 `segments` 테이블 정의에 발화 배열 필드 반영

### A2. 스키마 미결정 사항 확정 (정리노트 §6-1-1, progress.md §4)

**세부목표**: Supabase로 옮기기 전에 테이블에 구멍이 없는지 확정한다. (Phase B1의 선행조건)

- [ ] **[결정 필요] 인물 전거(person_authority_id) 필드** (정리노트 §6-1-1, §8-7)
  질문: "김OO"가 서로 다른 자료에 등장했을 때 동일인/동명이인을 구분할 방법이 스키마에 없음
  **제안**: 자료/구간/연표에 인물 태그를 붙일 때 참조하는 `persons` 노드(현재도 `persons_authority` 시트로 이미 운영 중, progress.md §3-1)에 `person_authority_id`(한국역대인물DB 등 외부 전거 ID, nullable)를 필드로만 미리 열어두고, 전수 매칭은 지금 하지 않는다 — 값이 없어도 동작에 지장 없게.
  완료 조건: 정리노트 §6-1-1 해당 문단 삭제, §6-1 스키마에 필드 반영

- [ ] **[결정 필요] 장소 좌표 확보 방식** (progress.md §4, §7-3 — 정리노트엔 아직 기록 안 됨)
  질문: `PlaceRef.lat/lng`를 누가/어떻게 채우나 — 수동 입력 vs 지오코딩 API
  **제안**: 1단계는 등록 화면에서 수동 입력(위도/경도 직접 타이핑 또는 지도 클릭 좌표 추출)로 시작. 자동 지오코딩 API는 신뢰도·요금 이슈가 있어 2단계로 미룸.
  완료 조건: 정리노트 §6-1에 `PlaceRef` 관련 정책 문단 신설 (현재 문서에 아예 없는 내용이므로 신규 추가)

- [ ] **[결정 필요] 공개범위(visibility_scope) 설계** (정리노트 §6-5)
  질문: 개인 연구용 온전 저장과 향후 "타인 공개" 시 저장 범위를 구분해야 하는데 현재 보류 상태
  **제안**: 1단계에서는 필드만 미리 만들어두되(`visibility_scope`, default `'private'`) 실제 분기 로직은 구현하지 않는다 — 2단계 착수 시점에 재설계.
  완료 조건: 정리노트 §6-5에 "필드는 선반영, 로직은 2단계 보류"로 명시

### A3. IA 공백 확정 (정리노트 §4, §8, §3-4)

**세부목표**: 화면을 새로 그리기 전에, "빠뜨렸다"고 적힌 개념들이 새 화면이 필요한 건지 기존 화면으로 흡수된 건지부터 결론낸다.

- [ ] **[결정 필요] 검색창(통합 검색) 화면** (정리노트 §4, §8-8, progress.md §6)
  질문: 구술 녹취록 자동추출 키워드 + 사용자 직접 입력 검색이 애초 기획에 있었는데 화면이 안 만들어짐
  **제안**: 별도 화면을 새로 만들지 않고, `SiteHeader`(현재 nav만 있음)에 전역 검색 인풋을 추가 — 입력 시 연표/구술목록 각각에 이미 존재하는 검색 로직(SegmentListClient, TimelineExperience)을 공유 상태로 끌어올려 두 목록에 동시 반영.
  완료 조건: 정리노트 §4 IA에 "공통 헤더 — 전역 검색" 블록 추가, §8-8 삭제

- [ ] **[결정 필요] "통합 타임라인" 개념의 행방** (정리노트 §4, §8-9)
  질문: 초기엔 "모든 자료가 하나의 시간순 타임라인" 그림이었는데, 화면이 연표목록/구술목록/사이드패널로 쪼개지면서 이 개념이 대체된 건지 별도로 더 필요한 건지 불명확
  **제안**: 대체된 것으로 결론 — `/timeline`(연표 목록 페이지)을 공식적으로 "통합 타임라인"의 구현체로 정의한다. 이미 사료·구술·연표가 한 화면(5컬럼)에 같이 배치되어 있어(progress.md §2-2) 원래 의도를 충족함. "통합 타임라인"이라는 용어는 문서에서 폐기하고 "연표"로 통일.
  완료 조건: 정리노트 §4, §8-9 삭제하고 §1 "어떻게" 문단에 "연표 화면이 통합 타임라인 역할을 겸함" 한 줄 추가

- [x] **[결정됨] UI/UX 레퍼런스(매거진형 카드 피드) 적용처** (정리노트 §3-4, §8-10) — 확정 완료
  "자료 피드"라는 화면이 폐기됐다고 단정하지 않기로 함 — 디자인 방향에 따라 다시 필요해질 수 있어 여전히 열린 문제. 현재는 "매거진형 카드 피드" + 사이드 패널 조합 방향이 나왔고, 목업 단계인 구술목록·연표 화면에 실험적으로 반영된 상태(최종 확정 아님)로 기록만 함.

- [ ] **[결정 필요] 사이드패널 "시대상 몰입감" 개선 방향** (정리노트 §8-1)
  질문: ① 오픈그래프 썸네일 확대, ② 서술적 프레이밍, ③ 콜라주 표현 — 3안 중 결론 없음
  **제안**: ①을 먼저 채택(구현 난이도 낮고 오픈그래프 파이프라인이 이미 부분적으로 있음, 정리노트 §5-2). ②는 ①과 병행 가능하니 같이 진행. ③(콜라주)은 난이도가 높으니 Phase 3 이후 재검토.
  완료 조건: 정리노트 §8-1을 "①②채택, ③보류"로 갱신

- [x] **[결정됨] 로그인하는 "다른 사람"의 권한 범위** (정리노트 §8-2) — 확정 완료
  질문은 애초에 잘못 던져진 것이었음 — "읽기 전용 vs 등록 권한"이라는 권한 레벨의 문제가 아니라, 애초에 여러 사람이 하나의 DB·화면을 공유하는 구조가 아니었음. 2단계에서 "다른 사람"이 쓴다는 건 API 연결 틀(엔진)은 같이 쓰되 각자 자기만의 DB·화면(개인화 인스턴스)을 갖는 것 — 정리노트 §1 "누구를", §2 "이용 형태" 반영 완료.
  남은 것: 이 개인화 인스턴스를 실제로 어떻게 배포할지(CollectionBuilder식 템플릿화 등, §3-2)는 여전히 미정 → B4로 이관

### A4. 외부데이터 이용정책 확정 (정리노트 §2, §3-5, §8-3, progress.md §3-2)

**세부목표**: 연동을 늘리기 전에, 실제 이용조건과 문서 오류부터 바로잡는다.

- [x] **[실행] 국립중앙도서관 관련 문서 오류 정정** — 완료
  정리노트 §3-5의 "국립중앙도서관 대한민국 신문 아카이브: LOD 제공"을 정정함 — 신문아카이브 자체는 API 없음(확인됨), LOD는 서지(도서) 데이터용 별도 서비스였음. (기존 §3-1 국내 유사서비스 벤치마킹 언급도 있었으나, §3-1 자체를 통째로 삭제하기로 결정함)

- [x] **[실행] 기관별 API·이용조건·저작권 정책 현황표 작성** — 완료 (정리노트 §3-6 신설)
  그동안 progress.md·정리노트 곳곳에 흩어져 있던 11개 기관의 접근방식·인증·이용조건·재배포가능여부를 한 표로 통합. "미확인" 상태인 항목이 아직 대부분이므로, 아래 실행과제로 이어짐.

- [ ] **[실행] §3-6 표의 "미확인" 항목 채우기** (우선순위: mkoha > encykorea > 나머지)
  - mkoha(현대한국구술자료관): 발췌 재게시 가능 여부 — 2단계 게이트의 핵심이라 최우선. 공식 문의 또는 이용약관 페이지 확인
  - encykorea(한국민족문화대백과사전): 키 발급 시 신청서에 명시한 이용범위와 실제 발급 조건 일치 확인
  - 국가기록원·국사편찬위(data.go.kr 계열): 공공데이터포털 표준 이용약관 조항 확인
  - 한국학자료센터·한국역대인물DB·문화공공데이터광장·KCI: 아직 실연동 전이라 우선순위 낮음, 실연동 착수 시점에 같이 확인
  완료 조건: 정리노트 §3-6 표의 "미확인" 셀을 실제 값으로 채움

- [ ] **[결정 필요] 2단계 재배포 범위 정책** (정리노트 §2 표 "주의사항")
  질문: 2단계(엔진 공유)로 확장할 때 각 연동 기관 자료를 재배포해도 되는지
  **제안**: 지금 당장 결정할 필요는 없음 — 다만 "2단계 착수 전 필수 게이트"로 명시해두고, 실제 조사는 목표 B4(Phase 4)에서 실행과제로 수행.
  완료 조건: 정리노트 §2 표에 "2단계 착수 전 재배포 조항 전수 확인 필수" 각주 추가

---

## 목표 B. 정책을 반영한 실행 로드맵

> 아래 Phase는 목표 A의 [결정 필요] 항목들이 확정된 뒤 순서대로 진행하는 것을 전제로 합니다. 특히 B1은 A1·A2 확정 없이는 스키마를 다시 갈아엎을 위험이 커서, A1·A2가 사실상 B1의 선행조건입니다.

### B1. Phase 1 — 스키마 확정 기반 Supabase 전환

- [ ] **[실행]** A1·A2에서 확정된 정책을 반영해 최종 테이블 스키마 확정 (`timeline_events`, `archive_items`, `segments`, `links`, `persons`, `places`)
- [ ] **[실행]** Supabase 프로젝트에 마이그레이션 SQL 작성·적용
- [ ] **[실행]** RLS(Row Level Security) 정책 설정 — 현재는 소유자 1인 기준이므로 단순하게
- [ ] **[실행]** `src/lib/real-data.ts`의 정적 데이터를 마이그레이션 스크립트로 DB에 이관
- [ ] **[실행]** 화면단 데이터 페칭을 정적 import에서 Supabase 클라이언트 호출로 교체

### B2. Phase 2 — 자료 등록/관리 워크플로우

- [ ] **[실행]** CSV 업로드 처리기 구현 (A1에서 확정한 오류처리 정책 반영)
- [ ] **[실행]** 자료 등록 폼 화면 — 링크 입력 → API/LOD 연동 시도 → 실패 시 오픈그래프 → 그것도 실패 시 수동 입력 (정리노트 §5-2)
- [ ] **[실행]** 구술 발췌구간 반복입력 UI (A1 발화 배열 구조 반영)
- [ ] **[실행]** 미연결 자료 모아보기 화면 (정리노트 §4)
- [ ] **[실행]** 로그인 화면 — 소유자 1인 인증만 (A3 결정에 따라 "다른 사람" 권한 로직은 제외)

### B3. Phase 3 — 커버리지 확장 + 경험 개선

- [ ] **[실행]** mkoha.aks.ac.kr 붙여넣기 마무리 — 전달된 표를 시트 반영 후 real-data.ts(→ DB)에 연결 (progress.md §7-1)
- [ ] **[실행]** 한국민족문화대백과사전 API 키 발급 확인 후 연동 (A4 이용조건 확보 후)
- [ ] **[실행]** A3에서 확정한 전역 검색 인풋을 SiteHeader에 추가
- [ ] **[실행]** A3에서 채택한 사이드패널 개선안(①②) 적용
- [ ] **[실행]** 발견 화면 재설계 여부 최종 결정 후 착수 (정리노트 IA 기준으로 부활시킬지, 연표가 흡수한 것으로 확정할지 — 이것도 사실 A3에 추가할 [결정 필요] 항목이나, 우선순위가 낮아 Phase 3 시점에 재논의)
- [ ] **[실행]** 웹검색 기반 2차 자료 "제안/참고" 포스트잇 UI (메모리 기록됨, progress.md §3-2)

### B4. Phase 4 — 2단계(엔진 공유) 준비

- [ ] **[실행]** §3-6 표의 "재배포·상업적 이용" 열 전수 확인 완료 (2단계 착수 필수 게이트, 정리노트 §2 주의사항)
- [ ] **[실행]** 개인화 인스턴스 배포 방식 설계 — A3에서 확정된 대로 "다른 사람"은 로그인 권한이 아니라 별도 인스턴스를 가지므로, 그 인스턴스를 어떻게 만들어주는지 설계 (CollectionBuilder처럼 CSV/설정 업로드 → 자동 배포하는 템플릿형, 정리노트 §3-2 참고)
- [ ] **[실행]** 공개범위(visibility_scope) 실제 로직 구현 (A2에서 필드만 선반영해둔 것 — 다만 개인화 인스턴스 구조에서는 애초에 인스턴스 자체가 분리되므로 이 필드의 필요성 자체를 재검토)

---

## 진행 현황

- **§1 서비스 개요(무엇을/왜/누구를/핵심 설계 원칙)는 이 로드맵과 별도로 대화를 통해 이미 재정리·반영 완료**했습니다 — 주제 중심 발췌+연결 구조, 학술적 근거(Portelli 1979 / Frisch & Lambert 2012, §3-3), 개인화 DB 기반 2단계 구조가 확정됨.
- A3의 "로그인 권한 범위", A4의 "국립중앙도서관 오류 정정"·"기관별 이용조건 표" 항목은 완료 처리했습니다.

## 다음 대화에서 할 일

남은 **[결정 필요]** 항목은 9개입니다 (A1 2개, A2 3개, A3 3개, A4 1개). 검토해서 승인/수정해 주시면 승인된 항목부터 정리노트에 즉시 반영하겠습니다. 전부 한 번에 볼 필요는 없고, Phase 1(B1) 착수에 필요한 A1·A2부터 먼저 확정하는 것을 권합니다.
